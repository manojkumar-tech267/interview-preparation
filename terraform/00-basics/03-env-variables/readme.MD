# export TF_VAR_instance_type=t3.micro
# unset TF_VAR_instance_type
# echo $TF_VAR_instance_type
# we can comment related to this variable in variables.tf file and terraform.tfvars file then only it will work